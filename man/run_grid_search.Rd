% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_grid_search.R
\name{run_grid_search}
\alias{run_grid_search}
\title{Run grid search over PV, battery, mode, and contract combinations}
\usage{
run_grid_search(
  df,
  params,
  pv_range,
  batt_range,
  modes = c("smart", "optimizer", "optimizer_lp", "optimizer_qp"),
  contrats = c("fixe", "dynamique"),
  baseline_mode = "thermostat",
  progress_fn = NULL
)
}
\arguments{
\item{df}{Raw data dataframe}

\item{params}{Base parameter list (PV, battery, etc. will be overridden
per scenario)}

\item{pv_range}{Numeric vector of PV capacities to test (kWc)}

\item{batt_range}{Numeric vector of battery capacities to test (kWh)}

\item{modes}{Character vector of optimisation modes. Use sidebar names:
\code{"smart"}, \code{"optimizer"}, \code{"optimizer_lp"},
\code{"optimizer_qp"}}

\item{contrats}{Character vector of contract types: \code{"fixe"},
\code{"dynamique"}}

\item{baseline_mode}{Baseline mode (default \code{"parametric"})}

\item{progress_fn}{Optional callback \code{function(k, total, detail)}
for progress reporting (e.g. Shiny progress bar). Default \code{NULL}.}
}
\value{
A tibble with columns: \code{PV_kWc}, \code{Batterie_kWh},
  \code{Mode}, \code{Contrat}, \code{Cout_EUR}, \code{Autoconso_pct},
  \code{Injection_kWh}, \code{Soutirage_kWh}, sorted by \code{Cout_EUR}
}
\description{
Sweeps over all combinations of PV capacity, battery size, optimisation
mode, and contract type, running a full simulation for each. Returns
results sorted by ascending net cost.
}
\examples{
\dontrun{
results <- run_grid_search(
  df, params,
  pv_range = seq(5, 15, by = 2),
  batt_range = c(0, 10),
  modes = c("smart", "optimizer"),
  contrats = "dynamique"
)
head(results)
}
}
