% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_ui_theme.R
\name{plot_overlay_bar}
\alias{plot_overlay_bar}
\title{Overlay Bar Chart (baseline vs optimised)}
\usage{
plot_overlay_bar(data, baseline_col, opti_col, ylab)
}
\arguments{
\item{data}{A dataframe with a \code{timestamp} column and the two series
to compare}

\item{baseline_col}{Character. Column name for the baseline series}

\item{opti_col}{Character. Column name for the optimised series}

\item{ylab}{Character. Y-axis label (e.g. \code{"kWh (horaire)"})}
}
\value{
A \pkg{plotly} object
}
\description{
Creates a stacked bar chart where for each period the bar height equals
\code{max(baseline, opti)}. The bottom portion (0 to min) is coloured as the
smaller series and the top portion (min to max) as the larger one.
This visually shows that one value is contained within the other.
}
\details{
No Shiny dependency — can be used standalone for reporting.
}
\examples{
df <- data.frame(
  timestamp = seq(as.POSIXct("2025-01-01"), by = "hour", length.out = 24),
  soutirage_baseline = runif(24, 1, 5),
  soutirage_opti = runif(24, 0.5, 4)
)
plot_overlay_bar(df, "soutirage_baseline", "soutirage_opti", "kWh")
}
