% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_simulation.R
\name{run_simulation}
\alias{run_simulation}
\title{Run full simulation pipeline}
\usage{
run_simulation(
  df,
  params,
  mode = "smart",
  baseline_mode = "parametric",
  fallback_mode = "smart"
)
}
\arguments{
\item{df}{Raw data dataframe (with timestamp, pv_kwh, t_ext, etc.)}

\item{params}{Parameter list (as returned by sidebar params_r or
SimulationParams)}

\item{mode}{Optimisation mode: \code{"smart"}, \code{"milp"}, \code{"lp"},
or \code{"qp"}}

\item{baseline_mode}{Baseline mode (default \code{"parametric"})}

\item{fallback_mode}{If optimisation fails, fall back to this mode
(default \code{"smart"}). Set to \code{NULL} to disable fallback.}
}
\value{
A named list with:
  \describe{
    \item{sim}{Simulation results dataframe}
    \item{df}{Baseline dataframe}
    \item{params}{Final parameter list (may differ from input due to
      data-dependent adjustments)}
    \item{mode}{Optimisation mode actually used}
  }
}
\description{
Executes the complete workflow: data preparation, parametric baseline,
optimisation, and baseline guard. This is the main entry point for
running a simulation outside of Shiny.
}
\examples{
\dontrun{
result <- run_simulation(df, params, mode = "milp")
result$sim  # optimised results
KPICalculator$new()$compute(result$sim, result$sim, result$params)
}
}
